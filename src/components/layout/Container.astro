---
export interface Props {
  class?: string;
  id?: string;
}

const { class: className = '', id } = Astro.props;
---

<div id={id} class:list={["w-full max-w-[1440px] mx-auto px-5 sm:px-8 lg:px-16", className]}>
  <slot />
</div>
